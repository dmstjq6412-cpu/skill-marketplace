# 팀 전용 코딩 규칙

이 파일에 팀만의 규칙을 추가하세요.
코드 리뷰 시 `code_review_rules.md`의 공통 기준과 함께 적용됩니다.

규칙은 CRITICAL(커밋 차단)과 WARNING(경고만) 중 하나로 지정하세요.

---

## CRITICAL (커밋 차단)

<!-- 팀 규칙 추가 예시:
- 특정 deprecated API 사용 금지
- 특정 라이브러리 직접 import 금지
- 특정 패턴 강제 금지

예시:
### 내부 DB 직접 접근 금지
직접 DB 쿼리 대신 반드시 Repository 레이어를 통해야 합니다.
위험: `db.query(...)` (서비스 레이어에서 직접 사용)
안전: `userRepository.findById(id)`
-->

(아직 추가된 규칙 없음)

---

## WARNING (경고만)

<!-- 팀 규칙 추가 예시:
- 특정 네이밍 컨벤션 권고
- 특정 패턴 사용 권고

예시:
### 한국어 주석 권장
영문 주석보다 한국어 주석을 권장합니다.
-->

(아직 추가된 규칙 없음)
